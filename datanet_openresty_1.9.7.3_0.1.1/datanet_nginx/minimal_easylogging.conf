* GLOBAL:
   FORMAT               =  "%datetime %msg"
   FILENAME             =  "/tmp/LOG_ZYNC_NGINX_AGENT_1"
   ENABLED              =  false
   TO_FILE              =  true
   TO_STANDARD_OUTPUT   =  false
   MILLISECONDS_WIDTH   =  6
   PERFORMANCE_TRACKING =  false
   LOG_FLUSH_THRESHOLD  =  100 ## Flush after every 100 logs

* ERROR:
   ENABLED              =  true
   FORMAT               = "%datetime %msg"

* FATAL:
   ENABLED              =  true
   FORMAT               = "%datetime %msg"
