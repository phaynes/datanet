* GLOBAL:
   FORMAT               =  "%datetime %msg"
   FILENAME             =  "/tmp/LOG_ZYNC_NGINX_AGENT_2"
   ENABLED              =  true
   TO_FILE              =  true
   TO_STANDARD_OUTPUT   =  false
   MILLISECONDS_WIDTH   =  6
   PERFORMANCE_TRACKING =  false
   LOG_FLUSH_THRESHOLD  =  100 ## Flush after every 100 logs

* DEBUG:
   TO_STANDARD_OUTPUT   =  true
   FORMAT               = "%msg"

* INFO:
   TO_STANDARD_OUTPUT   =  true
   FORMAT               = "%msg"

* ERROR:
   TO_STANDARD_OUTPUT   =  true
   FORMAT               = "%msg"

* FATAL:
   TO_STANDARD_OUTPUT   =  true
   FORMAT               = "%msg"
